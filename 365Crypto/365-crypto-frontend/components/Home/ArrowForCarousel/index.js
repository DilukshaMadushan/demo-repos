import React from 'react';
import styles from './index.module.css';

const ArrowForCarousel = () => {
	return (
		<div className={styles.arrowButtonDiv}>
			<div className={styles.arrowButton}>*</div>
		</div>
	);
};

export default ArrowForCarousel;
